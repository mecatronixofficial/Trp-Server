import { Body, Controller, Get, Post, Query, UseGuards } from '@nestjs/common';
import { CurrentUser } from '../auth/decorators/current-user.decorator'; import { Roles } from '../auth/decorators/roles.decorator'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { RolesGuard } from '../auth/guards/roles.guard'; import { Role } from '../common/enums'; import { DailyClosingService } from './daily-closing.service';
@UseGuards(JwtAuthGuard, RolesGuard) @Roles(Role.SUPER_ADMIN, Role.ADMIN) @Controller('daily-closing')
export class DailyClosingController { constructor(private service: DailyClosingService) {} @Get() list(@CurrentUser() user: any, @Query('date') date?: string) { return this.service.list(user, date); } @Post('close') close(@CurrentUser() user: any, @Body() dto: { date: string; branch?: string }) { return this.service.close(user, dto.branch, dto.date); } @Post('reopen') reopen(@CurrentUser() user: any, @Body() dto: { date: string; branch?: string }) { return this.service.reopen(user, dto.branch, dto.date); } }
